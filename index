<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CEFR A1 Vocabulary Extractor</title>
  <style>
    body {
      font-family: 'Comic Sans MS', cursive;
      background-color: #f0f9ff;
      text-align: center;
      padding: 20px;
    }
    h1 {
      color: #0077b6;
      margin-bottom: 10px;
    }
    textarea {
      width: 80%;
      height: 120px;
      font-size: 16px;
      margin-bottom: 10px;
      padding: 10px;
      border: 2px solid #90e0ef;
      border-radius: 8px;
    }
    button {
      background: #90e0ef;
      border: none;
      padding: 10px 20px;
      font-size: 18px;
      cursor: pointer;
      border-radius: 10px;
      margin-bottom: 20px;
    }
    button:hover {
      background: #48cae4;
    }
    .vocab-box {
      display: inline-block;
      background: #caf0f8;
      border-radius: 10px;
      padding: 15px;
      margin: 10px;
      font-size: 24px;
      font-weight: bold;
      color: #03045e;
    }
    footer {
      margin-top: 40px;
      font-size: 14px;
      color: #888;
    }
  </style>
</head>
<body>

<h1>🌟 CEFR A1 Vocabulary Extractor</h1>
<p>Paste your story below:</p>

<textarea id="text" placeholder="Paste your story here..."></textarea><br>
<button onclick="extractVocab()">Extract Vocabulary</button>

<div id="results"></div>

<footer>Created by Young Learners Curriculum © 2025</footer>

<script>
// Static CEFR A1 word list (real words ≤5 letters)
const cefrWords = new Set([
  'apple','house','table','chair','water',
  'happy','small','green','light','child',
  'smile','heart','dream','world','family',
  'learn','clean','brown','white','black',
  'grass','story','sound','book','pen',
  'bag','mouse','train','plane','cloud',
  'tree','fish','bird','sun','moon',
  'milk','eggs','bread','fruit'
]);

// Exclude articles and common function words
const excludedWords = new Set([
  'a','an','the','and','but','or','so','if','for','nor','yet','with','from','that'
]);

function extractVocab() {
  const text = document.getElementById('text').value.toLowerCase().trim();
  if (!text) {
    alert('Paste your story to extract words.');
    return;
  }

  // Clean and split
  const tokens = text.replace(/[^a-z\s]/g, ' ').split(/\s+/);

  // Filter: length 2–5, in A1 list, not excluded
  const filtered = tokens.filter(w =>
    w.length >= 2 && w.length <= 5 &&
    cefrWords.has(w) &&
    !excludedWords.has(w)
  );

  // Unique and shuffle
  const unique = Array.from(new Set(filtered));
  for (let i = unique.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [unique[i], unique[j]] = [unique[j], unique[i]];
  }

  // Select up to 4
  const selected = unique.slice(0, 4);

  // Display
  const container = document.getElementById('results');
  container.innerHTML = '';
  if (selected.length === 0) {
    container.innerHTML = '<p>No matching words found.</p>';
    return;
  }

  selected.forEach(w => {
    const div = document.createElement('div');
    div.className = 'vocab-box';
    div.textContent = w.toUpperCase();
    container.appendChild(div);
  });
}
</script>

</body>
</html>
